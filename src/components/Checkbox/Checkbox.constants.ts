import type { CheckboxSize, CheckboxVariant, CheckboxLabelPosition } from './Checkbox.types';

export const DEFAULT_CHECKBOX_SIZE: CheckboxSize = 'md';
export const DEFAULT_CHECKBOX_VARIANT: CheckboxVariant = 'default';
export const DEFAULT_LABEL_POSITION: CheckboxLabelPosition = 'right';

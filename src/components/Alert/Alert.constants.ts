import type { AlertSize, AlertVariant } from './Alert.types';

export const DEFAULT_ALERT_VARIANT: AlertVariant = 'info';
export const DEFAULT_ALERT_SIZE: AlertSize = 'md';
export const DEFAULT_AUTO_CLOSE_DELAY = 5000;
